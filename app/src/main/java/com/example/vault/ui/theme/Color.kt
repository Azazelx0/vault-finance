package com.example.vault.ui.theme

import androidx.compose.ui.graphics.Color

// Backgrounds
val DeepVoid = Color(0xFF0F172A)
val SurfaceLayer = Color(0xFF1E293B)

// Primaries
val EmeraldGrowth = Color(0xFF10B981)
val RoseAlert = Color(0xFFF43F5E)
val RoyalAccent = Color(0xFF6366F1)

// Neutrals
val StarlightText = Color(0xFFF8FAFC)
val MistText = Color(0xFF94A3B8)
val Outline = Color(0xFF334155)

// Light Theme variants (Optional / TBD - focusing on Dark/Premium first as per brief)
// For now, mapping same colors or slightly tweaked for light mode if needed later.
